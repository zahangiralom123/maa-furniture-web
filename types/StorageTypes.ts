export type storageInfo = {
    maxStorage: number,
    usedStorage : number,
    remainingStorage : number,
    maxStorageMB: number,
    usedStorageMB: number,
    remainingStorageMB: number,
     imageCount: number
}